#include <iostream>
#include <iomanip>
#include <string>
#include <stdio.h>
#include <math.h>
#include <stack>
using namespace std;
int main()
{
	int a,temp1;
	scanf("%d",&a);
	char input[400],rearranged[400],characters[400],expressions[400];
	for(int j=0;j<a;j++)
	{
		temp1=0;
		scanf("%s",&input);
		for (int i = 0; i < count; ++i)
		{
			/* code */
			if(input[i]=='(')
			{

			}
			else if (input[i]==')')
			{

			}
			else if (input[i]=='+'||input[i]=='-'||input[i]=='/'||input[i]=='*'||input[i]=='^')
			{

			}
			else
			{
				

			}

		}
	}